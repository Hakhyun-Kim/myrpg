# 서버 (기본값은 전부 로컬 개발용 — 아무것도 설정하지 않아도 돈다)
PORT=7777            # HTTP+WebSocket 포트
HOST=0.0.0.0
DATA_DIR=./data      # 파일 스토리지 위치 (클라우드 이전 시 Postgres 어댑터로 교체)
AUTOSAVE_SEC=30

# 클라이언트 빌드 (미설정 시: dev는 ws://localhost:7777/ws, prod는 접속한 host 자동 사용)
# VITE_SERVER_URL=wss://game.example.com/ws
